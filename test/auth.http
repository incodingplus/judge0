POST http://localhost:2358/submissions/batch?base64_encoded=false&wait=true
Content-Type: application/json

{
    "submissions":[
        {
            "source_code":"#include <iostream>\nint main() {\n    if constexpr (true) {\n        std::cout << \"C++20 feature: if constexpr works!\" << std::endl;\n    }\n    return 0;\n}",
            "language_id":54,
            "compiler_options":"-std=c++20"
        },
        {
            "source_code":"#include <iostream>\nint main() {\n    if constexpr (true) {\n        std::cout << \"C++20 feature: if constexpr works!\" << std::endl;\n    }\n    return 0;\n}",
            "language_id":54,
            "compiler_options":"-std=c++20"
        }
    ]
}